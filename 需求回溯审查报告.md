# 云海观测决策台 — 需求回溯审查报告

> **项目路径**: `miniprogram/`  
> **审查日期**: 2025年  
> **审查方式**: 基于源代码的逆向需求分析（非文档驱动）  
> **版本**: 当前代码库最新状态

---

## 一、功能清单梳理（基于代码实现）

### 1.1 页面结构

| 页面 | 路径 | 功能定位 |
|------|------|----------|
| 首页（决策台） | `pages/index/index` | 核心功能聚合页：天气数据获取、云海/晚霞/星空预测、摄影参数、反馈收集 |
| 观测历史 | `pages/history/history` | 历史预测记录查看、准确率统计、CSV 导出 |

### 1.2 核心功能矩阵

#### A. 位置与定位（`services.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| A1 | 自动 GPS 定位 | ✅ 已实现 | `index.js:autoLocate()`, `services.js:getLocation()` |
| A2 | 地名搜索（中文地名解析） | ✅ 已实现 | `services.js:geocodeAddress()` — 支持省/市/区/景区拆分 |
| A3 | 搜索结果多选（ActionSheet） | ✅ 已实现 | `index.js:onSearch()` — 多结果时弹出选择 |
| A4 | 地图点击选位 | ✅ 已实现 | `index.js:onMapTap()` — 有坐标校验 |
| A5 | 地图展示（当前位置标记） | ✅ 已实现 | `index.wxml` — 使用微信原生 map 组件 |

#### B. 天气数据（`services.js`, `calculations.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| B1 | 海拔获取 | ✅ 已实现 | `services.js:fetchElevation()` — Open-Meteo API |
| B2 | 天气数据获取（多模式） | ✅ 已实现 | `services.js:fetchWeather()` — ICON + GFS 模式 |
| B3 | 数据缓存（10 分钟 TTL） | ✅ 已实现 | `services.js:getCachedData/setCachedData()` |
| B4 | 请求重试机制 | ✅ 已实现 | `services.js:wxRequest()` — 1 次重试 |
| B5 | 多日预报（日期切换） | ✅ 已实现 | `index.js:onDayChange()` — picker 选择器 |
| B6 | 当前天气指标展示 | ✅ 已实现 | 温度/体感温度/湿度/风速/云量/海拔/云底高度/露点差 |
| B7 | 逐小时预报（可折叠） | ✅ 已实现 | `index.wxml` — 温度/云底/降水概率 |

#### C. 云海预测（`calculations.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| C1 | 云海评分（0-100） | ✅ 已实现 | `calculations.js:analyzeCloudSeaSample()` |
| C2 | 多因子打分模型 | ✅ 已实现 | 湿度(25)+海拔差(25)+能见度(15)+风速(10)+云量(8)+低云(12)+露点差(12)+气压(5)+时段(10)+逆温(8)-降水惩罚(15) |
| C3 | 云底高度估算 | ✅ 已实现 | `cloudBaseFromDewPoint()` / `cloudBaseFromHumidity()` — 125×(T-Td) 公式 |
| C4 | 逆温层检测 | ✅ 已实现 | `scoreInversion()` — 逐小时温度递增检测 |
| C5 | 置信度标签 | ✅ 已实现 | `scoreToConfidence()` — 高把握/较高/一般/较低 |
| C6 | 评分理由生成 | ✅ 已实现 | `buildReasons()` — 最多 5 条文字说明 |
| C7 | 当日最佳小时分析 | ✅ 已实现 | `analyzeDayCloudSea()` — 遍历 24 小时取最高分 |

#### D. 晚霞/火烧云预测（`sunset.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| D1 | 晚霞评分模型 | ✅ 已实现 | `analyzeGlowSample()` — 中层云(28)+高层云(18)+湿度(12)+能见度(12)+时段(20)-低云惩罚(20)-降水惩罚(15) |
| D2 | 日出/日落霞光分别评分 | ✅ 已实现 | `analyzeDayGlow()` — 分离 sunrise/sunset 最佳时段 |
| D3 | 日落时间窗口评分 | ✅ 已实现 | `scoreSunsetWindow()` / `scoreSunriseWindow()` |
| D4 | 火烧云等级判定 | ✅ 已实现 | 高/中/低/极低 四档 |

#### E. 银河/星空预测（`stargazing.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| E1 | 月相计算（朔望周期法） | ✅ 已实现 | `getMoonAge()` — 基于 2000-01-06 参考新月 |
| E2 | 月面亮度百分比 | ✅ 已实现 | `getMoonIllumination()` — 余弦近似 |
| E3 | 银河核心可见性（季节+纬度） | ✅ 已实现 | `getMilkyWayVisibility()` — 北/南半球分支 |
| E4 | 星空综合评分 | ✅ 已实现 | 月相(20)+云量(20)+通透度(15)+湿度(10)+海拔(10)+银河季节(10) |
| E5 | 天文摄影参数（500法则/NPF） | ✅ 已实现 | `astroShutter()`, `getAstroParams()` |

#### F. 摄影参数推荐（`photography.js`, `camera-presets.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| F1 | 光照阶段判定（10 种状态） | ✅ 已实现 | `getLightingPhase()` — 蓝调/金色日出/日间/夜间 等 |
| F2 | EV 值估算 | ✅ 已实现 | `estimateEV()` — 基于光照阶段+云量+能见度+海拔 |
| F3 | 单反/微单通用参数 | ✅ 已实现 | `generateCameraParams()` — 光圈/快门/ISO/白平衡/焦段 |
| F4 | 手机拍摄通用参数 | ✅ 已实现 | `generatePhoneParams()` — 模式/镜头/技巧 |
| F5 | 设备特定推荐（12 款相机） | ✅ 已实现 | Canon 5D4/R5/R6II, Sony A7RV/A7C2/A6700, Nikon Z8/Z6III, Fuji X-T5 |
| F6 | 设备特定推荐（8 款手机） | ✅ 已实现 | iPhone 16 Pro, 华为 P70, 小米 15, 一加 13, OPPO Find X8, vivo X200, 三星 S25U, Pixel 9 |
| F7 | 滤镜建议 | ✅ 已实现 | `generateFilterRecs()` — GND/CPL/ND |
| F8 | 景深/超焦距计算 | ✅ 已实现 | `calculateDepthOfField()` |
| F9 | 拍摄时间轴 | ✅ 已实现 | `generateTimeline()` — 基于日出日落分段 |
| F10 | ND 减光计算器 | ✅ 已实现 | `generateNDCalc()` |
| F11 | 延时摄影建议 | ✅ 已实现 | `generateTimelapseRec()` — 间隔/时长/张数/成片长度 |
| F12 | 曝光参数表（类 Planit） | ✅ 已实现 | `generateExposureTable()` — 多场景对照表 |
| F13 | 构图建议 | ✅ 已实现 | `generateCompositionTips()` |
| F14 | 日月方位 | ✅ 已实现 | `celestial` 数据块 — 日出/日落时间与方向 |

#### G. 多模式融合预测（`fusion.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| G1 | 4 模式并行获取 | ✅ 已实现 | ICON/GFS/JMA/ECMWF — `Promise.allSettled` |
| G2 | 加权平均评分 | ✅ 已实现 | ECMWF 权重 1.5 > JMA 1.2 > ICON/GFS 1.0 |
| G3 | 模式一致性（标准差） | ✅ 已实现 | σ ≤5 高一致/≤12 基本一致/>12 分歧大 |
| G4 | 时段共识 | ✅ 已实现 | 取最佳时段众数 |
| G5 | 逆温层共识 | ✅ 已实现 | 多数模式检测到逆温 = 共识 |
| G6 | 各模式得分明细 | ✅ 已实现 | 可折叠展示 |
| G7 | 优雅降级 | ✅ 已实现 | 部分模式失败不影响整体 |

#### H. 安全功能
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| H1 | 雷暴风险预警（CAPE 指标） | ✅ 已实现 | `analyzer.js:buildSafetyAlerts()` — CAPE ≥500/1000 |
| H2 | 低温提醒 | ✅ 已实现 | 体感温度 < 5°C 提醒 |
| H3 | 高海拔提醒 | ✅ 已实现 | 海拔 > 1500m 防晒补水提醒 |
| H4 | SOS 紧急求救 | ✅ 已实现 | `index.js:onSOS()` — 拨打 110/119 + 复制位置信息 |

#### I. 用户反馈系统（`feedback.js`）
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| I1 | 预测快照自动保存 | ✅ 已实现 | `autoSaveFeedback()` — 每次渲染天气时保存 |
| I2 | 实际观测结果填报 | ✅ 已实现 | 三态切换（null→true→false→null） |
| I3 | 用户评分（1-5 星） | ✅ 已实现 | `onSetRating()` |
| I4 | 文字备注 | ✅ 已实现 | `onFeedbackNoteInput()` |
| I5 | 预测准确率统计 | ✅ 已实现 | `getFeedbackStats()` — 云海/晚霞/银河分项准确率 |
| I6 | CSV 导出（复制到剪贴板） | ✅ 已实现 | `exportFeedbackCSV()` |
| I7 | 同日同地点去重 | ✅ 已实现 | `saveFeedback()` — 0.001° 容差 |
| I8 | 最多保留 200 条 | ✅ 已实现 | `saveFeedback()` — 超出自动截断 |

#### J. 决策辅助
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| J1 | 英雄决策卡片 | ✅ 已实现 | `renderWeather()` — 大片日/云海有戏/晚霞概率高/适合拍银河/在家修图 |
| J2 | 行动建议（出发等级） | ✅ 已实现 | `buildObservationGuidance()` — 值得冲/可以蹲守/可观望/不建议 |
| J3 | 推荐观测时段 | ✅ 已实现 | 基于日出 ±1-2 小时窗口 |
| J4 | 机位海拔建议 | ✅ 已实现 | `recommendedViewpoint()` + `recommendedTargetElevation()` |

#### K. 社交与分享
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| K1 | 微信分享（转发消息） | ✅ 已实现 | `onShareAppMessage()` — 含地点名+评分 |

#### L. 交互体验
| 编号 | 功能 | 实现状态 | 代码位置 |
|------|------|----------|----------|
| L1 | 加载骨架屏 | ✅ 已实现 | `index.wxml` — skeleton 动画 |
| L2 | 底部固定操作栏 | ✅ 已实现 | 摄影参数/反馈/历史/求救/分享 5 按钮 |
| L3 | 弹出面板（摄影/反馈） | ✅ 已实现 | 底部上滑 Panel + 遮罩 |
| L4 | 可折叠区块（逐小时/融合） | ✅ 已实现 | `onToggleHourly()`, `onToggleFusion()` |
| L5 | 安全区域适配 | ✅ 已实现 | `env(safe-area-inset-bottom)` |
| L6 | 状态横幅 | ✅ 已实现 | 加载中/成功/警告 三态 |

---

## 二、需求遗漏分析

### 2.1 高优先级遗漏（P0 — 核心体验缺失）

| 编号 | 缺失功能 | 影响分析 | 建议 |
|------|----------|----------|------|
| M1 | **收藏地点/常用地点** | 用户每次都需重新搜索或定位，高频观测点（如牛背山、黄山）无法快速切换 | 支持收藏地点列表，一键切换 |
| M2 | **推送通知/订阅消息** | 用户无法被动获知云海条件变好。核心场景"明早有云海，现在出发"无法覆盖 | 接入微信订阅消息，当评分 ≥ 阈值时推送 |
| M3 | **离线使用能力** | 山区无网络时完全不可用。天气缓存仅 10 分钟，登山途中大概率过期 | 延长缓存 TTL + 离线降级页面 |
| M4 | **下拉刷新** | 缺少标准微信下拉刷新，用户刷新数据无明显入口（仅靠重新搜索） | 添加 `onPullDownRefresh()` |

### 2.2 中优先级遗漏（P1 — 专业用户期待）

| 编号 | 缺失功能 | 影响分析 |
|------|----------|----------|
| M5 | **光污染地图/Bortle 等级** | 银河观测模块没有光污染评估，仅用海拔代替，城市高海拔（如重庆南山）会产生误导 |
| M6 | **多日趋势对比视图** | 当前只能逐天查看，无法直观对比"未来 3 天哪天最好" |
| M7 | **历史天气回溯** | 无法查看过去日期的实际天气与预测对比，缺失学习反馈闭环 |
| M8 | **海拔手动修正** | `fetchElevation()` 返回的是地表海拔，但摄影者可能在山顶观景台（有数十米人为高差）。无手动调整入口 |
| M9 | **朝向/方位角建议** | 知道"日出方向"但缺少基于地形的"最佳朝向"建议，也无指南针集成 |
| M10 | **天气雷达/卫星云图** | 专业观云用户依赖实时卫星云图判断云层运动方向 |
| M11 | **用户引导/新手教程** | 复杂专业参数缺少使用引导（如什么是露点差、逆温层） |
| M12 | **多语言支持** | 所有文本硬编码中文，无法服务海外华人或外国摄影师 |

### 2.3 低优先级遗漏（P2 — 锦上添花）

| 编号 | 缺失功能 | 影响分析 |
|------|----------|----------|
| M13 | **社区/照片上传** | 缺少 UGC 机制，用户无法分享观测成果 |
| M14 | **个人偏好设置** | 无法自定义评分阈值、温度单位（°C/°F）、距离单位 |
| M15 | **装备清单** | 根据天气条件自动生成出行装备建议（三脚架/保暖衣物/雨具/头灯等） |
| M16 | **路线规划/导航** | 知道目标海拔但无法直接导航到推荐观测点 |
| M17 | **Apple Watch / 手表小组件** | 山地场景掏手机不便，需穿戴设备快速查看核心指标 |
| M18 | **数据导出为图片/长图** | 分享时仅转发小程序卡片，无法生成精美的预测分享图 |

---

## 三、验收准则回溯（Given/When/Then）

### 3.1 云海预测核心流程

```gherkin
Feature: 云海评分预测
  作为一名山岳摄影师
  我希望根据气象条件获得云海出现概率的量化评分
  以便决定是否出发拍摄

  Scenario: 高分云海条件
    Given 用户位于海拔 1800m 的山区
      And 当前湿度 > 90%
      And 露点差 < 2°C
      And 云底高度估算 < 1600m（低于观测点）
      And 风速 < 5 m/s
      And 无降水
    When 系统完成天气分析
    Then 云海评分应 ≥ 70
      And 置信度应显示"高把握"
      And 英雄卡片应显示"☁️ 云海有戏"或"🔥 大片日"

  Scenario: 低分条件
    Given 用户位于海拔 200m 的平原
      And 湿度 < 50%
      And 露点差 > 8°C
    When 系统完成天气分析
    Then 云海评分应 < 35
      And 置信度应显示"较低"
      And 英雄卡片应显示"😴 今天适合在家修图"

  Scenario: 降水惩罚
    Given 任意基础条件的云海评分为 X
      And 降水概率 ≥ 80% 或降水量 ≥ 2mm
    When 系统计算评分
    Then 最终评分应 ≤ X - 10（最多扣 15 分）
```

### 3.2 地点搜索

```gherkin
Feature: 地名搜索与地理编码
  Scenario: 复合地名解析
    Given 用户输入"四川甘孜州牛背山"
    When 系统执行地名搜索
    Then 应按"牛背山" → "甘孜州" → "四川" 逐级尝试
      And 返回结果应优先匹配四川省
      And 结果列表去重后不超过 8 条

  Scenario: 搜索无结果
    Given 用户输入"xxxxxxxxx"
    When 系统执行地名搜索
    Then 应显示"未找到该地址，请尝试更简短的核心地名"

  Scenario: 搜索多结果
    Given 用户输入"泰山"
    When 返回多个结果
    Then 弹出 ActionSheet 供用户选择（最多 6 条）
      And 用户取消选择后显示"已取消选择"
```

### 3.3 晚霞预测

```gherkin
Feature: 晚霞/火烧云预测
  Scenario: 理想晚霞条件
    Given 中层云量 30-70%
      And 高层云量 20-60%
      And 低层云量 < 30%
      And 时段在日落前 30 分钟至日落后 15 分钟
      And 湿度 40-75%
      And 能见度 > 15km
    When 系统评估霞光潜力
    Then 晚霞评分应 ≥ 70
      And 标签应显示"火烧云概率高"
```

### 3.4 银河/星空预测

```gherkin
Feature: 银河观测条件评估
  Scenario: 最佳观星条件
    Given 月龄接近 0（新月）
      And 云量 < 10%
      And 能见度 > 20km
      And 湿度 < 40%
      And 海拔 > 2000m
      And 日期在 6-8 月
    When 系统评估星空条件
    Then 星空评分应 ≥ 70
      And 标签应显示"极佳观星条件"
      And 银河核心应标记为"可见"
      And 应提供推荐观测时段

  Scenario: 满月不利条件
    Given 月龄接近 14.8（满月）
    When 系统计算月相
    Then 月面亮度应 ≈ 100%
      And 月相得分应为 0
      And 理由中应提示"月光较强，不利于银河/星空拍摄"
```

### 3.5 摄影参数推荐

```gherkin
Feature: 设备特定摄影参数
  Scenario: 选择相机型号后获得推荐
    Given 用户打开摄影参数面板
      And 选择相机"Canon R5"
      And 云海评分 ≥ 55 且风速 ≤ 5 m/s
    When 系统生成设备推荐
    Then 应推荐 RF 15-35mm f/2.8 广角镜头
      And 光圈设为 f/8-f/11
      And ISO 设为 100
      And 快门建议慢门 (0.5-4s)
      And 应同时推荐长焦备选镜头 RF 70-200mm

  Scenario: 夜间条件
    Given 光照阶段为"night"
    When 系统生成参数
    Then 光圈应为最大光圈
      And ISO 应为 1600-3200
      And 快门应为 15-30s
```

### 3.6 多模式融合

```gherkin
Feature: 多模式天气融合预测
  Scenario: 所有模式成功
    Given ICON/GFS/JMA/ECMWF 四个模式均成功获取
    When 系统计算融合评分
    Then 融合评分应为加权平均（ECMWF 权重 1.5 最高）
      And 模式一致性标准差 ≤ 5 时标记"模式高度一致"
      And 显示各模式独立评分明细

  Scenario: 部分模式失败
    Given ECMWF 和 JMA 请求失败
      And ICON 和 GFS 请求成功
    When 系统计算融合
    Then 应基于 2 个成功模式计算融合评分
      And 模式数量应显示"2 个"
      And 不应报错或崩溃
```

### 3.7 SOS 紧急求救

```gherkin
Feature: 紧急求救
  Scenario: 触发 SOS
    Given 用户位于坐标 (30.5678, 103.1234), 海拔 2800m
    When 用户点击 SOS 按钮
    Then 弹出选项：拨打 110 / 拨打 119 / 复制位置信息
      And 复制内容应包含：坐标(6位小数)、海拔、地名、当前时间
```

### 3.8 反馈系统

```gherkin
Feature: 预测反馈与准确率
  Scenario: 提交观测反馈
    Given 系统已自动保存今日预测快照
    When 用户打开反馈面板
      And 标记"云海已出现""晚霞未出现"
      And 给予 4 星评分
      And 填写备注"云量偏薄但出现时间准确"
      And 点击提交
    Then 反馈数据应持久化到本地 Storage
      And 弹出"反馈已保存 ✓"

  Scenario: 准确率计算
    Given 历史 10 条记录中：
      - 8 条云海预测评分 ≥ 55 且实际出现（true positive）
      - 2 条云海预测评分 ≥ 55 但实际未出现（false positive）
    When 系统计算云海准确率
    Then 准确率应为 80%（8/10 × 100）

  Scenario: 同日同地去重
    Given 今天已有北京(39.90, 116.41)的记录
    When 再次查看北京天气触发自动保存
    Then 不应创建新记录，应更新已有记录的预测部分
```

---

## 四、边界条件审查

### 4.1 已处理的边界条件 ✅

| 边界条件 | 处理方式 | 代码位置 |
|----------|----------|----------|
| 天气 API 全部失败 | 两个 Endpoint 轮询 + 重试 + 错误提示 | `services.js:fetchWeather()` |
| 定位权限被拒 | fallback 到默认北京坐标 | `index.js:autoLocate()` |
| 海拔 API 失败 | 返回默认 300m | `services.js:fetchElevation()` |
| 地图点击坐标无效 | 校验 lat ∈ [-90,90], lon ∈ [-180,180] | `index.js:onMapTap()` |
| 多模式全部失败 | 静默降级，不显示融合区块 | `index.js:fetchFusion()` |
| 天气数据格式异常 | 检查 `data?.hourly?.time?.length` | `services.js` |
| NaN/undefined 数值 | `Number(x ?? 0)` 全覆盖 | `calculations.js` 全文 |
| 反馈记录超限 | 截断到 200 条 | `feedback.js:saveFeedback()` |
| CSS 安全区域 | `env(safe-area-inset-bottom)` | `index.wxss` |

### 4.2 未处理的边界条件 ❌

| 编号 | 边界条件 | 风险 | 严重程度 |
|------|----------|------|----------|
| E1 | **南半球用户** | 银河可见性逻辑简化为 `coreVisible = month >= 1 && month <= 10`，准确性存疑；云海时段窗口假设日出在凌晨，南半球冬季日出可能到 8:00+ | 中 |
| E2 | **极地/高纬度地区** | 极昼/极夜时日出日落计算可能返回 undefined，导致光照阶段判定为 `unknown` | 中 |
| E3 | **海拔 ≤ 0（盆地/死海）** | `gapToElevation` 计算可能为负数百，`recommendedTargetElevation` 可能返回负值 | 低 |
| E4 | **存储空间不足** | `wx.setStorageSync` 无 try-catch（反馈保存时），微信 Storage 限制 10MB 可能导致数据丢失 | 中 |
| E5 | **时区非 Asia/Shanghai** | API 请求硬编码 `timezone=Asia/Shanghai`，海外用户日出日落时间可能错误 | 高 |
| E6 | **搜索空字符串/特殊字符** | `geocodeAddress` 对空串有检查，但 URL 注入（如 `%00`）或超长输入未过滤 | 低 |
| E7 | **网络从有到无的切换** | 缓存 TTL 10 分钟。用户在有网时获取数据后进山，10 分钟后缓存失效，再次操作会失败且无离线模式 | 高 |
| E8 | **日期切换超出天气数据范围** | `selectedDayIndex` 通过 picker 选择，但如果天气数据不足 3 天（API 返回数据偏少），`start = dayIndex * 24` 可能越界导致空数据 | 中 |
| E9 | **并发刷新冲突** | 连续快速点搜索/定位按钮，多个 `fetchAll` 并发执行，最终渲染结果可能不一致（旧请求晚于新请求返回） | 中 |
| E10 | **feedback ID 碰撞** | `id: 'fb_' + Date.now()` — 同一毫秒调用两次会产生重复 ID | 低 |
| E11 | **CSV 导出数据量过大** | 200 条记录生成的 CSV 文本可能超出剪贴板限制或造成 UI 卡顿 | 低 |
| E12 | **相机/手机 picker 索引越界** | `this.data.cameraPresets[idx]?.id` 使用了可选链，但若 idx 为 NaN 或负数未防护 | 低 |

---

## 五、用户故事补全

以下功能在代码中已实现但缺少明确的用户故事定义，现予补全：

### US-01: 英雄决策卡片
```
作为一名周末登山摄影师
我希望打开小程序后立刻看到一句简洁的决策建议（如"🔥 大片日"）
以便在 3 秒内判断今天是否值得出发
验收标准：
  - 云海≥70 且晚霞≥60 → "大片日！双绝"
  - 云海≥55 → "云海有戏"
  - 晚霞≥60 → "晚霞概率较高"
  - 星空≥60 → "今晚适合拍银河"
  - 以上均不满足 → "在家修图"
```

### US-02: 逐小时预报折叠
```
作为普通用户
我不需要看每小时的详细数据
所以逐小时预报默认折叠，只在我想深入了解时展开
以避免信息过载
```

### US-03: 多模式融合折叠 + 摘要
```
作为专业气象爱好者
我希望在折叠状态下看到融合评分和一致性的简要摘要
以便在不展开的情况下评估预测可信度
展开后可以看到每个模式的独立评分和详细分析
```

### US-04: SOS 紧急求救
```
作为一名独自登山的户外爱好者
在遇到紧急情况（迷路、受伤、恶劣天气）时
我希望一键拨打紧急电话并自动准备我的精确位置信息
以便救援人员能快速定位我
```

### US-05: 摄影参数弹窗面板
```
作为一名正在山顶拍摄的摄影师
我需要在不离开主页面的情况下快速查阅曝光参数
所以摄影参数作为底部弹出面板而非独立页面
我可以随时滑出查看，不丢失当前天气上下文
```

### US-06: 反馈系统（预测 vs 实际）
```
作为产品团队
我们需要收集"预测评分 vs 实际观测"的对照数据
以便持续验证和优化评分模型
数据以简单的三态开关（出现/未出现/未填写）收集
降低用户填写成本
```

### US-07: CSV 导出到剪贴板
```
作为一名数据分析爱好者
我希望导出所有历史反馈数据
但微信小程序不支持文件下载
所以将 CSV 文本复制到剪贴板，我粘贴到备忘录或文件中使用
```

### US-08: 天气数据缓存
```
作为频繁切换查看的用户
我不希望每次操作都触发 API 请求（浪费流量且慢）
所以天气数据缓存 10 分钟、海拔数据缓存 24 小时
缓存使用时应提示"使用缓存数据"
```

---

## 六、I18n（国际化）审查

### 6.1 当前状态：❌ 完全不具备国际化能力

**所有用户可见文本均为硬编码中文字符串**，分布在以下位置：

#### WXML 模板硬编码文本（部分列举）
| 文件 | 示例文本 |
|------|----------|
| `index.wxml:4` | `CLOUD SEA FORECAST LAB`（英文硬编码） |
| `index.wxml:5` | `云海观测决策台` |
| `index.wxml:6` | `面向摄影、徒步与山地观测的专业天气研判工具` |
| `index.wxml:8` | `输入地点（默认：北京）` |
| `index.wxml:81` | `湿度` / `风速` / `云量` / `海拔` / `云底高度` / `露点差` |
| `index.wxml:109` | `FORECAST ASSESSMENT` / `云海判断` |
| `index.wxml:126` | `⚠️ 预测仅供参考...` |
| `index.wxml:351` | `🆘 SOS 紧急求救` |
| `history.wxml` | `📊 观测历史` / `预测记录与准确性趋势` / `导出` |

#### JS 逻辑层硬编码文本（部分列举）
| 文件 | 示例文本 |
|------|----------|
| `calculations.js:134` | `高把握` / `较高把握` / `一般` / `较低` |
| `calculations.js:188` | `当前位置已具备优势，优先选择山顶或高位观景台。` |
| `calculations.js:274` | `检测到逆温层（温差 X°C），有利于低云/雾层稳定维持。` |
| `sunset.js:170-173` | `火烧云概率高` / `晚霞潜力较好` / `可能有色彩` / `晚霞概率低` |
| `stargazing.js:41-49` | `新月` / `蛾眉月` / `上弦月` 等月相名称 |
| `stargazing.js:188-191` | `极佳观星条件` / `较好观星条件` / `一般观星条件` / `不利于观星` |
| `feedback.js:98` | CSV 表头 `日期,地点,纬度,经度,...` |
| `services.js:210` | `未找到该地址，请尝试更简短的核心地名` |
| `camera-presets.js` 全文 | 所有相机/手机的描述文本 |

#### 配置文件
| 文件 | 文本 |
|------|------|
| `app.json:9` | `navigationBarTitleText: "云海观测决策台"` |
| `app.json:15` | `desc: "用于获取您的当前位置以查询附近天气和云海预测"` |
| `history.json:3` | `navigationBarTitleText: "观测历史"` |

### 6.2 I18n 改造建议

1. **提取文本资源文件**: 建立 `utils/i18n.js` + `locales/zh-CN.json` / `locales/en-US.json`
2. **模板绑定**: 将硬编码文本替换为 `{{t.featureName}}`，通过 `setData` 注入当前语言包
3. **日期/数字格式化**: 当前使用 `toLocaleString('zh-CN')`，需参数化 locale
4. **专业术语**: 气象/摄影术语需专业翻译（如"逆温层" = "temperature inversion"）
5. **单位系统**: 温度(°C/°F)、距离(m/ft)、风速(m/s/mph/kn) 需可切换
6. **API 参数**: `geocodeAddress` 的 `language=zh` 参数需动态化
7. **急救电话**: SOS 功能的 110/119 需按用户所在国家/地区适配

### 6.3 工作量估算

| 项目 | 预估文本条目数 | 备注 |
|------|----------------|------|
| WXML 模板 | ~120 条 | 含标签、占位符、提示 |
| JS 逻辑层 | ~200 条 | 含评分理由、建议文本 |
| 设备预设 | ~150 条 | 相机/手机描述与技巧 |
| 配置文件 | ~5 条 | |
| **合计** | **~475 条** | 首期建议仅做中英双语 |

---

## 附录：技术债与优化建议

| 编号 | 项目 | 描述 |
|------|------|------|
| T1 | `index.js` 体积 | 487 行，职责仍偏重（事件处理 + 数据流转 + 反馈管理），可进一步拆分 |
| T2 | `calculations.js` 体积 | 19.3KB，包含评分引擎 + 指导建议 + 工具函数，建议拆分 |
| T3 | 测试缺失 | 零测试代码。评分模型（核心业务逻辑）无单元测试保障 |
| T4 | `clamp`/`lerp` 重复 | `calculations.js` 和 `sunset.js` 各自定义了同名函数 |
| T5 | 错误处理不一致 | 部分 `catch` 静默忽略，部分显示 Toast，缺少统一错误处理策略 |
| T6 | 无日志上报 | 所有错误仅 `console.warn`，生产环境无法追踪故障 |

---

> **审查结论**: 该小程序功能覆盖面广（云海+晚霞+星空三维预测 + 专业摄影参数 + 多模式融合 + 安全预警 + 反馈闭环），核心算法设计合理。主要差距在于：①缺少收藏地点/推送通知等高频使用场景支持；②离线能力和边界条件防护不足；③完全不具备国际化能力；④零测试覆盖带来质量风险。
